# PWA cœur — plan d'implémentation

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Rendre l'application installable sur Android/Chrome, instantanée au second lancement, et consultable hors-ligne sur les pages déjà visitées.

**Architecture:** Un service worker maison, écrit en TypeScript, compilé par un second build Vite vers `public/sw-runtime.js`, puis servi à la racine par la route Blade `pwa.sw` qui lui préfixe les constantes calculées côté PHP (`CACHE_VERSION` = hash du manifest Vite, `PRECACHE_URLS`, `OFFLINE_URL`). Toute la logique décidable vit dans des fonctions pures sous `resources/js/lib/`, testées avec Vitest. Côté client, un module réactif unique expose l'état PWA et un bandeau flottant, monté comme application Vue distincte, le rend visible.

**Tech Stack:** Laravel 13, Inertia Laravel 3.3, Inertia Vue 3.6, Vue 3.5, Vite 7, Vitest 4, Pest 5 (+ pest-plugin-browser 5), Tailwind 4, TypeScript 5.7, bun.

**Spec:** `docs/superpowers/specs/2026-08-18-pwa-coeur-design.md`

## Global Constraints

- **Cible unique : Android / Chrome.** Aucune balise `apple-mobile-web-app-*`, aucun contournement iOS.
- **Aucune dépendance ajoutée.** Ni `vite-plugin-pwa`, ni Workbox, ni rien d'autre. Tout ce qui est utilisé est déjà dans `composer.json` / `package.json`.
- **Tout texte visible est en français**, comme le reste de l'interface.
- **Commentaires et noms de tests en français**, conformément au dépôt (`describe('pageContainer', ...)`, `it('rend un conteneur centré...')`).
- **Types de retour explicites** sur toutes les fonctions TypeScript et PHP, et types de paramètres partout.
- **`vendor/bin/pint --dirty --format agent`** après toute modification de fichier PHP.
- **Accolades obligatoires** sur toutes les structures de contrôle PHP, même à corps unique.
- **`CACHE_VERSION` provient exclusivement de `Vite::manifestHash()`** — la même valeur qu'`HandleInertiaRequests::version()`. Ne jamais la calculer autrement.
- **Le worker est servi depuis la racine** (`/sw.js`), jamais depuis `/build/`. Son scope doit rester `/`.
- **Le préfixe des URLs d'assets est `/build/`** (répertoire de build par défaut de `laravel-vite-plugin`).
- **Nom du cache : `argent-{CACHE_VERSION}`.** Tout cache dont le nom diffère est supprimé à l'activation.
- **Pas de `skipWaiting()` automatique.** Il n'est appelé qu'en réponse au message `SKIP_WAITING` envoyé par le client.
- **Convention hors-ligne : `rescuedProps` d'Inertia v3**, jamais une convention `null` maison.

---

## Structure des fichiers

**Créés**

| Fichier | Responsabilité |
| --- | --- |
| `resources/views/pwa/offline.blade.php` | Document HTML autonome affiché quand une URL jamais visitée est demandée hors-ligne. |
| `app/Shared/Pwa/ServiceWorkerScript.php` | Calcule `CACHE_VERSION` et `PRECACHE_URLS` depuis le manifest Vite, lit le runtime compilé, assemble le script final. |
| `resources/js/lib/swCache.ts` | Fonctions pures : classification des requêtes, clés de cache, payload `rescuedProps`, extraction du `data-page`. |
| `resources/js/lib/swCache.test.ts` | Tests Vitest des fonctions ci-dessus. |
| `resources/js/pwa/sw.ts` | Point d'entrée du worker : cycle de vie, gestionnaire `fetch`, diffusion. |
| `vite.sw.config.ts` | Second build, sortie `public/sw-runtime.js` sans hash. |
| `resources/js/lib/serviceWorker.ts` | État réactif PWA côté client et pilotage de la mise à jour et de l'installation. |
| `resources/js/lib/serviceWorker.test.ts` | Tests Vitest du module ci-dessus. |
| `resources/js/components/AppServiceWorkerBanner.vue` | Bandeau flottant, trois états exclusifs. |
| `resources/js/pwa/banner.ts` | Monte le bandeau sur `#pwa-banner` et démarre l'enregistrement. |
| `tests/Feature/PwaRoutesTest.php` | Tests des routes `manifest.json`, `sw.js`, `hors-ligne` et du layout. |
| `tests/Browser/PwaTest.php` | Enregistrement du worker et remplissage du cache dans un vrai navigateur. |
| `tests/pwa-offline.mjs` | Script Playwright autonome pour le parcours hors-ligne. |

**Modifiés**

| Fichier | Modification |
| --- | --- |
| `resources/views/pwa/meta-tags.blade.php` | Actuellement vide : reçoit le lien manifest, `theme-color`, l'icône. |
| `resources/views/pwa/sw.blade.php` | Actuellement vide : rend le script assemblé. |
| `resources/views/app.blade.php` | Inclut les méta-balises, ajoute `<div id="pwa-banner">`. |
| `routes/pwa.php` | Ajoute la route `hors-ligne` ; la route `sw.js` passe par `ServiceWorkerScript`. |
| `package.json` | Le script `build` enchaîne les deux builds Vite. |
| `resources/js/app.ts` | Appelle `mountServiceWorkerBanner()`. |
| `resources/js/components/ValueVsInvestedChart.vue` | Ajoute le slot `#rescue`. |
| `resources/js/components/dashboard/PerformancesSection.vue` | Ajoute le slot `#rescue`. |
| `resources/js/components/dashboard/SectorsSection.vue` | Ajoute le slot `#rescue`. |
| `resources/js/components/instrument/PriceHistorySection.vue` | Ajoute le slot `#rescue`. |
| `resources/js/components/dashboard/InstrumentsSection.vue` | Lit `rescuedProps` pour éteindre son squelette. |

---

## Task 1 : Manifest, méta-balises et page hors-ligne

**Files:**
- Create: `resources/views/pwa/offline.blade.php`
- Create: `tests/Feature/PwaRoutesTest.php`
- Modify: `resources/views/pwa/meta-tags.blade.php` (fichier vide)
- Modify: `resources/views/app.blade.php`
- Modify: `routes/pwa.php`

**Interfaces:**
- Consomme : `config('pwa.*')` et la route nommée `pwa.manifest`, tous deux déjà en place.
- Produit : la route nommée `pwa.offline` répondant sur `/hors-ligne`, consommée par la Task 3 (`OFFLINE_URL`) ; le nœud `<div id="pwa-banner">` consommé par la Task 5.

- [ ] **Step 1: Écrire le test qui échoue**

Créer `tests/Feature/PwaRoutesTest.php` :

```php
<?php

it('sert un manifest conforme à la configuration', function () {
    $this->get('/manifest.json')
        ->assertOk()
        ->assertJsonPath('short_name', config('pwa.short_name'))
        ->assertJsonPath('display', 'standalone')
        ->assertJsonPath('start_url', '/')
        ->assertJsonPath('icons.0.sizes', '192x192')
        ->assertJsonPath('icons.1.sizes', '512x512');
});

it('sert une page hors-ligne autonome', function () {
    $this->get('/hors-ligne')
        ->assertOk()
        ->assertSee('Pas de connexion')
        ->assertSee('Les données affichées');
});

it('déclare le manifest et le point de montage du bandeau dans le layout', function () {
    $this->get('/')
        ->assertOk()
        ->assertSee('rel="manifest"', false)
        ->assertSee('name="theme-color"', false)
        ->assertSee('id="pwa-banner"', false);
});
```

- [ ] **Step 2: Lancer le test pour vérifier qu'il échoue**

Run: `php artisan test --compact --filter=PwaRoutes`
Expected: FAIL — `/hors-ligne` renvoie une redirection ou 404, et les assertions `rel="manifest"` / `id="pwa-banner"` ne trouvent rien.

- [ ] **Step 3: Écrire les méta-balises**

Remplacer le contenu de `resources/views/pwa/meta-tags.blade.php` :

```blade
<link rel="manifest" href="{{ route('pwa.manifest') }}">
<meta name="theme-color" content="{{ config('pwa.theme_color') }}">
<link rel="icon" type="image/png" sizes="192x192" href="/icons/icon-192x192.png">
<link rel="icon" type="image/png" sizes="512x512" href="/icons/icon-512x512.png">
```

- [ ] **Step 4: Écrire la page hors-ligne**

Créer `resources/views/pwa/offline.blade.php`. Document autonome, styles en ligne : cette page doit s'afficher même si aucune feuille de style n'a été mise en cache.

```blade
<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <meta name="color-scheme" content="light dark">
        <title>Hors-ligne — {{ config('pwa.name') }}</title>
        <style>
            :root { color-scheme: light dark; }
            body {
                margin: 0;
                min-height: 100dvh;
                display: flex;
                flex-direction: column;
                align-items: center;
                justify-content: center;
                gap: 0.75rem;
                padding: 2rem;
                text-align: center;
                font-family: system-ui, -apple-system, sans-serif;
                background: Canvas;
                color: CanvasText;
            }
            h1 { margin: 0; font-size: 1.0625rem; font-weight: 700; }
            p { margin: 0; max-width: 32ch; font-size: 0.875rem; opacity: 0.7; }
        </style>
    </head>
    <body>
        <h1>Pas de connexion</h1>
        <p>Les données affichées proviennent du cache. Cette page n'a pas encore été consultée, elle n'est donc pas disponible hors-ligne.</p>
    </body>
</html>
```

- [ ] **Step 5: Déclarer la route hors-ligne**

Dans `routes/pwa.php`, ajouter après la route `pwa.manifest` :

```php
/** Repli du service worker : une page jamais visitée n'a rien en cache à servir. */
Route::view('hors-ligne', 'pwa.offline')->name('pwa.offline');
```

- [ ] **Step 6: Brancher le layout**

Dans `resources/views/app.blade.php`, ajouter l'inclusion dans le `<head>`, juste avant `@vite(...)` :

```blade
        @include('pwa.meta-tags')

        @vite(['resources/css/app.css', 'resources/js/app.ts'])
```

et le point de montage dans le `<body>`, après `@inertia` :

```blade
    <body>
        @inertia

        {{-- Hors de l'application Inertia : les pages sont des racines indépendantes, sans layout partagé. --}}
        <div id="pwa-banner"></div>
    </body>
```

- [ ] **Step 7: Lancer les tests pour vérifier qu'ils passent**

Run: `php artisan test --compact --filter=PwaRoutes`
Expected: PASS — 3 tests.

- [ ] **Step 8: Formater et committer**

```bash
vendor/bin/pint --dirty --format agent
git add resources/views/pwa/meta-tags.blade.php resources/views/pwa/offline.blade.php resources/views/app.blade.php routes/pwa.php tests/Feature/PwaRoutesTest.php
git commit -m "feat: déclare le manifest PWA et la page hors-ligne"
```

---

## Task 2 : Logique de cache pure

**Files:**
- Create: `resources/js/lib/swCache.ts`
- Create: `resources/js/lib/swCache.test.ts`

**Interfaces:**
- Consomme : rien.
- Produit, tous importés par `resources/js/pwa/sw.ts` en Task 3 :
  - `type RequestShape = { method: string; url: string; mode: string; inertia: boolean; partialData: string | null }`
  - `type SwRequestKind = 'passthrough' | 'asset' | 'inertia' | 'navigation' | 'other'`
  - `type InertiaPage = { component: string; props: Record<string, unknown>; url: string; version: string | null; rescuedProps?: string[]; [key: string]: unknown }`
  - `classifyRequest(request: RequestShape): SwRequestKind`
  - `cacheKeyFor(request: RequestShape): string`
  - `partialKeysOf(partialData: string | null): string[]`
  - `rescuedPartialPayload(page: InertiaPage, requestedKeys: string[]): InertiaPage`
  - `pagePayloadFromDocument(html: string): InertiaPage | null`

- [ ] **Step 1: Écrire les tests qui échouent**

Créer `resources/js/lib/swCache.test.ts` :

```ts
import { describe, expect, it } from 'vitest';
import {
    cacheKeyFor,
    classifyRequest,
    pagePayloadFromDocument,
    partialKeysOf,
    rescuedPartialPayload,
    type InertiaPage,
    type RequestShape,
} from '@/lib/swCache';

const shape = (overrides: Partial<RequestShape> = {}): RequestShape => ({
    method: 'GET',
    url: 'https://argent.test/',
    mode: 'navigate',
    inertia: false,
    partialData: null,
    ...overrides,
});

describe('classifyRequest', () => {
    it('laisse passer tout ce qui n\'est pas un GET', () => {
        expect(classifyRequest(shape({ method: 'POST' }))).toBe('passthrough');
    });

    it('reconnaît un asset de build à son préfixe', () => {
        expect(classifyRequest(shape({ url: 'https://argent.test/build/assets/app-BR1IIldu.css' })))
            .toBe('asset');
    });

    it('reconnaît une requête Inertia avant la navigation', () => {
        expect(classifyRequest(shape({ inertia: true, mode: 'navigate' }))).toBe('inertia');
    });

    it('reconnaît une navigation de document', () => {
        expect(classifyRequest(shape({ mode: 'navigate' }))).toBe('navigation');
    });

    it('range le reste à part', () => {
        expect(classifyRequest(shape({ url: 'https://argent.test/icons/icon-192x192.png', mode: 'no-cors' })))
            .toBe('other');
    });
});

describe('cacheKeyFor', () => {
    it('distingue les trois formes d\'une même URL', () => {
        const document = cacheKeyFor(shape());
        const full = cacheKeyFor(shape({ inertia: true }));
        const partial = cacheKeyFor(shape({ inertia: true, partialData: 'catalog,trends' }));

        expect(new Set([document, full, partial]).size).toBe(3);
    });

    it('laisse l\'URL nue pour un document', () => {
        expect(cacheKeyFor(shape())).toBe('https://argent.test/');
    });

    it('trie les noms de props pour qu\'un ordre différent donne la même clé', () => {
        expect(cacheKeyFor(shape({ inertia: true, partialData: 'trends,catalog' })))
            .toBe(cacheKeyFor(shape({ inertia: true, partialData: 'catalog, trends' })));
    });

    it('conserve la chaîne de requête de la page', () => {
        const key = cacheKeyFor(shape({ url: 'https://argent.test/?range=ytd', inertia: true }));

        expect(key).toContain('range=ytd');
    });
});

describe('partialKeysOf', () => {
    it('découpe, nettoie et trie les noms de props', () => {
        expect(partialKeysOf(' trends , catalog ,')).toEqual(['catalog', 'trends']);
    });

    it('renvoie une liste vide quand l\'en-tête est absent', () => {
        expect(partialKeysOf(null)).toEqual([]);
    });
});

describe('rescuedPartialPayload', () => {
    const page: InertiaPage = {
        component: 'Dashboard',
        props: { overview: { total: 10 } },
        url: '/',
        version: 'abc',
    };

    it('marque comme rescapée chaque prop demandée qui manque', () => {
        const payload = rescuedPartialPayload(page, ['catalog', 'trends']);

        expect(payload.rescuedProps).toEqual(['catalog', 'trends']);
    });

    it('ne renvoie que les props demandées qui sont disponibles', () => {
        const payload = rescuedPartialPayload(
            { ...page, props: { overview: { total: 10 }, trends: [] } },
            ['catalog', 'trends'],
        );

        expect(payload.props).toEqual({ trends: [] });
        expect(payload.rescuedProps).toEqual(['catalog']);
    });

    it('conserve le composant et la version de la page en cache', () => {
        const payload = rescuedPartialPayload(page, ['catalog']);

        expect(payload.component).toBe('Dashboard');
        expect(payload.version).toBe('abc');
    });
});

describe('pagePayloadFromDocument', () => {
    it('extrait et déséchappe le data-page du document', () => {
        const html = '<div id="app" data-page="{&quot;component&quot;:&quot;Dashboard&quot;,'
            + '&quot;props&quot;:{},&quot;url&quot;:&quot;/&quot;,&quot;version&quot;:&quot;abc&quot;}"></div>';

        expect(pagePayloadFromDocument(html)?.component).toBe('Dashboard');
    });

    it('renvoie null quand le document ne porte pas de data-page', () => {
        expect(pagePayloadFromDocument('<html><body></body></html>')).toBeNull();
    });

    it('renvoie null quand le data-page n\'est pas du JSON valide', () => {
        expect(pagePayloadFromDocument('<div data-page="pas du json"></div>')).toBeNull();
    });
});
```

- [ ] **Step 2: Lancer les tests pour vérifier qu'ils échouent**

Run: `bunx vitest run resources/js/lib/swCache.test.ts`
Expected: FAIL — « Failed to resolve import "@/lib/swCache" ».

- [ ] **Step 3: Écrire l'implémentation**

Créer `resources/js/lib/swCache.ts` :

```ts
/**
 * Décisions de cache du service worker, isolées de l'API `Request` / `Cache` pour rester
 * testables sous happy-dom. Le worker n'y apporte que la traduction depuis les vrais objets.
 */

/** Répertoire de build de `laravel-vite-plugin` : ses URLs sont hashées, donc immuables. */
const BUILD_PREFIX = '/build/';

export type SwRequestKind = 'passthrough' | 'asset' | 'inertia' | 'navigation' | 'other';

/** Ce que le worker retient d'une requête pour décider quoi en faire. */
export type RequestShape = {
    method: string;
    url: string;
    mode: string;
    inertia: boolean;
    partialData: string | null;
};

/** Charge utile d'une page Inertia, réduite aux champs que le worker manipule. */
export type InertiaPage = {
    component: string;
    props: Record<string, unknown>;
    url: string;
    version: string | null;
    rescuedProps?: string[];
    [key: string]: unknown;
};

export function classifyRequest(request: RequestShape): SwRequestKind {
    if (request.method !== 'GET') {
        return 'passthrough';
    }

    if (new URL(request.url).pathname.startsWith(BUILD_PREFIX)) {
        return 'asset';
    }

    /** Avant la navigation : une visite Inertia porte elle aussi `mode: navigate`. */
    if (request.inertia) {
        return 'inertia';
    }

    return request.mode === 'navigate' ? 'navigation' : 'other';
}

export function partialKeysOf(partialData: string | null): string[] {
    if (partialData === null) {
        return [];
    }

    return partialData
        .split(',')
        .map((key: string): string => key.trim())
        .filter((key: string): boolean => key !== '')
        .sort();
}

/**
 * L'API Cache indexe sur l'URL seule, or `/` produit trois réponses distinctes selon les
 * en-têtes. La clé synthétique les sépare ; elle ne sert jamais au `fetch` réel.
 */
export function cacheKeyFor(request: RequestShape): string {
    const url = new URL(request.url);

    if (!request.inertia) {
        return url.toString();
    }

    const keys = partialKeysOf(request.partialData);

    url.searchParams.set('__sw', keys.length === 0 ? 'inertia' : `partial:${keys.join(',')}`);

    return url.toString();
}

/**
 * Réponse partielle de secours : les props demandées que la page en cache ne porte pas sont
 * listées dans `rescuedProps`, ce qui fait rendre le slot `#rescue` d'Inertia plutôt que de
 * laisser un squelette tourner indéfiniment.
 */
export function rescuedPartialPayload(page: InertiaPage, requestedKeys: string[]): InertiaPage {
    const props: Record<string, unknown> = {};
    const rescued: string[] = [];

    for (const key of requestedKeys) {
        if (key in page.props) {
            props[key] = page.props[key];

            continue;
        }

        rescued.push(key);
    }

    return {
        ...page,
        props,
        rescuedProps: [...new Set([...(page.rescuedProps ?? []), ...rescued])].sort(),
    };
}

const HTML_ENTITIES: Record<string, string> = {
    '&quot;': '"',
    '&#039;': "'",
    '&#39;': "'",
    '&lt;': '<',
    '&gt;': '>',
    '&amp;': '&',
};

/**
 * Dernier recours quand aucune réponse Inertia complète n'est en cache : le document HTML
 * porte la même charge utile dans son attribut `data-page`.
 */
export function pagePayloadFromDocument(html: string): InertiaPage | null {
    const match = /data-page="([^"]*)"/.exec(html);

    if (match === null) {
        return null;
    }

    /** `&amp;` en dernier, sinon il ré-introduit les entités qu'on vient de résoudre. */
    const json = match[1].replace(
        /&quot;|&#0?39;|&lt;|&gt;|&amp;/g,
        (entity: string): string => HTML_ENTITIES[entity] ?? entity,
    );

    try {
        return JSON.parse(json) as InertiaPage;
    } catch {
        return null;
    }
}
```

- [ ] **Step 4: Lancer les tests pour vérifier qu'ils passent**

Run: `bunx vitest run resources/js/lib/swCache.test.ts`
Expected: PASS — 17 tests.

- [ ] **Step 5: Vérifier les types**

Run: `bun run typecheck`
Expected: aucune erreur.

- [ ] **Step 6: Committer**

```bash
git add resources/js/lib/swCache.ts resources/js/lib/swCache.test.ts
git commit -m "feat: isole la logique de cache du service worker"
```

---

## Task 3 : Runtime du worker, second build, route `/sw.js`

**Files:**
- Create: `resources/js/pwa/sw.ts`
- Create: `vite.sw.config.ts`
- Create: `app/Shared/Pwa/ServiceWorkerScript.php`
- Modify: `resources/views/pwa/sw.blade.php` (fichier vide)
- Modify: `routes/pwa.php`
- Modify: `package.json`
- Test: `tests/Feature/PwaRoutesTest.php` (créé en Task 1)

**Interfaces:**
- Consomme : `classifyRequest`, `cacheKeyFor`, `partialKeysOf`, `rescuedPartialPayload`, `pagePayloadFromDocument`, `RequestShape`, `InertiaPage` (Task 2) ; la route `pwa.offline` (Task 1).
- Produit :
  - `App\Shared\Pwa\ServiceWorkerScript::render(): string` — script complet ou worker inerte.
  - Les messages diffusés `{ type: 'FRESH' }` et `{ type: 'SERVED_STALE', cachedAt: number | null }`, consommés en Task 4.
  - Le message accepté `{ type: 'SKIP_WAITING' }`, envoyé en Task 4.

- [ ] **Step 1: Écrire les aides de test**

Deux fichiers de `public/` décident du comportement testé, et un test qui les modifie sans les rendre casse les suivants. Ajouter ces aides à la fin de `tests/Pest.php` :

```php
/**
 * `public/hot` fait basculer Vite en mode développement : `manifestHash()` devient nul et la
 * route sert le worker inerte. Les tests PWA ont besoin des assets construits.
 *
 * @return string|null Contenu à rendre à `restoreViteHotFile()`.
 */
function hideViteHotFile(): ?string
{
    $path = public_path('hot');

    if (! Illuminate\Support\Facades\File::exists($path)) {
        return null;
    }

    $contents = Illuminate\Support\Facades\File::get($path);
    Illuminate\Support\Facades\File::delete($path);

    return $contents;
}

function restoreViteHotFile(?string $contents): void
{
    if ($contents !== null) {
        Illuminate\Support\Facades\File::put(public_path('hot'), $contents);
    }
}

/**
 * Le runtime compilé est un artefact de build : un test qui le supprime doit le remettre, sinon
 * les tests navigateur suivants n'obtiennent plus qu'un worker inerte.
 *
 * @return string|null Contenu à rendre à `restoreServiceWorkerRuntime()`.
 */
function hideServiceWorkerRuntime(): ?string
{
    $path = public_path('sw-runtime.js');

    if (! Illuminate\Support\Facades\File::exists($path)) {
        return null;
    }

    $contents = Illuminate\Support\Facades\File::get($path);
    Illuminate\Support\Facades\File::delete($path);

    return $contents;
}

function restoreServiceWorkerRuntime(?string $contents): void
{
    $path = public_path('sw-runtime.js');

    if ($contents === null) {
        Illuminate\Support\Facades\File::delete($path);

        return;
    }

    Illuminate\Support\Facades\File::put($path, $contents);
}
```

- [ ] **Step 2: Écrire le test qui échoue**

Ajouter à `tests/Feature/PwaRoutesTest.php` :

```php
use Illuminate\Support\Facades\File;
use Illuminate\Support\Facades\Vite;

it('sert un worker inerte tant que le runtime n\'est pas compilé', function () {
    $runtime = hideServiceWorkerRuntime();

    try {
        $this->get('/sw.js')
            ->assertOk()
            ->assertHeader('Content-Type', 'application/javascript')
            ->assertHeader('Cache-Control', 'no-cache, private')
            ->assertSee('unregister');
    } finally {
        restoreServiceWorkerRuntime($runtime);
    }
});

it('injecte la version de cache et les URLs à précacher dans le worker', function () {
    $hot = hideViteHotFile();
    $runtime = hideServiceWorkerRuntime();
    File::put(public_path('sw-runtime.js'), '/* runtime compilé */');

    try {
        $body = $this->get('/sw.js')->assertOk()->getContent();

        expect(Vite::manifestHash())->not->toBeNull();
        expect($body)
            ->toContain('self.CACHE_VERSION = '.json_encode(Vite::manifestHash()))
            ->toContain('/hors-ligne')
            ->toContain('/build/')
            ->toContain('/* runtime compilé */');
    } finally {
        restoreServiceWorkerRuntime($runtime);
        restoreViteHotFile($hot);
    }
});
```

- [ ] **Step 3: Lancer le test pour vérifier qu'il échoue**

Run: `php artisan test --compact --filter=PwaRoutes`
Expected: FAIL — la réponse `/sw.js` est vide, aucune des chaînes attendues n'est présente.

- [ ] **Step 4: Écrire l'assembleur PHP**

Créer `app/Shared/Pwa/ServiceWorkerScript.php` :

```php
<?php

namespace App\Shared\Pwa;

use Illuminate\Support\Facades\File;
use Illuminate\Support\Facades\Vite;

/**
 * Assemble le script servi à `/sw.js` : les constantes calculées côté PHP, puis le runtime
 * compilé par `vite.sw.config.ts`.
 */
class ServiceWorkerScript
{
    /**
     * Worker de repli en développement et tant que le second build n'a pas tourné : il se
     * retire et vide ses caches, plutôt que de servir des assets périmés pendant le HMR.
     */
    private const INERT = <<<'JS'
        self.addEventListener('install', () => self.skipWaiting());
        self.addEventListener('activate', (event) => {
            event.waitUntil((async () => {
                const names = await caches.keys();
                await Promise.all(names.map((name) => caches.delete(name)));
                await self.registration.unregister();
            })());
        });
        JS;

    public function render(): string
    {
        $version = Vite::manifestHash();
        $runtimePath = public_path('sw-runtime.js');

        if ($version === null || ! File::exists($runtimePath)) {
            return self::INERT;
        }

        $constants = implode("\n", [
            'self.CACHE_VERSION = '.json_encode($version).';',
            'self.PRECACHE_URLS = '.json_encode($this->precacheUrls()).';',
            'self.OFFLINE_URL = '.json_encode(route('pwa.offline', absolute: false)).';',
        ]);

        return $constants."\n".File::get($runtimePath);
    }

    /**
     * Tout ce que le manifest Vite déclare, plus les deux documents dont le worker a besoin
     * pour répondre hors-ligne. Les pages sont code-splittées : sans précache, un chunk jamais
     * demandé manque au premier accès hors-ligne après un déploiement.
     *
     * @return list<string>
     */
    private function precacheUrls(): array
    {
        /** @var array<string, array{file: string, css?: list<string>}> $manifest */
        $manifest = json_decode(File::get(public_path('build/manifest.json')), true);

        $urls = ['/', route('pwa.offline', absolute: false)];

        foreach ($manifest as $entry) {
            $urls[] = '/build/'.$entry['file'];

            foreach ($entry['css'] ?? [] as $stylesheet) {
                $urls[] = '/build/'.$stylesheet;
            }
        }

        return array_values(array_unique($urls));
    }
}
```

- [ ] **Step 5: Brancher la vue et la route**

Remplacer le contenu de `resources/views/pwa/sw.blade.php` par une seule ligne — la vue ne porte aucune logique :

```blade
{!! $script !!}
```

Dans `routes/pwa.php`, remplacer la fermeture de la route `sw.js` :

```php
use App\Shared\Pwa\ServiceWorkerScript;

Route::get('sw.js', function (ServiceWorkerScript $script) {
    return response()
        ->view('pwa.sw', ['script' => $script->render()])
        ->header('Content-Type', 'application/javascript')
        ->header('Cache-Control', 'no-cache');
})->name('pwa.sw');
```

- [ ] **Step 6: Écrire le runtime du worker**

Créer `resources/js/pwa/sw.ts` :

```ts
/// <reference lib="webworker" />

import {
    cacheKeyFor,
    classifyRequest,
    pagePayloadFromDocument,
    partialKeysOf,
    rescuedPartialPayload,
    type InertiaPage,
    type RequestShape,
} from '@/lib/swCache';

/** Constantes préfixées par `ServiceWorkerScript` : elles ne viennent pas du bundle. */
declare const self: ServiceWorkerGlobalScope & {
    CACHE_VERSION: string;
    PRECACHE_URLS: string[];
    OFFLINE_URL: string;
};

export type SwMessage =
    | { type: 'FRESH' }
    | { type: 'SERVED_STALE'; cachedAt: number | null };

const CACHE_NAME = `argent-${self.CACHE_VERSION}`;

/** Horodatage du dernier succès réseau, porté par la réponse en cache elle-même. */
const CACHED_AT_HEADER = 'X-Sw-Cached-At';

const shapeOf = (request: Request): RequestShape => ({
    method: request.method,
    url: request.url,
    mode: request.mode,
    inertia: request.headers.get('X-Inertia') !== null,
    partialData: request.headers.get('X-Inertia-Partial-Data'),
});

const broadcast = async (message: SwMessage): Promise<void> => {
    const clients = await self.clients.matchAll({ type: 'window' });

    for (const client of clients) {
        client.postMessage(message);
    }
};

const stamped = (response: Response): Response => {
    const headers = new Headers(response.headers);
    headers.set(CACHED_AT_HEADER, String(Date.now()));

    return new Response(response.body, {
        status: response.status,
        statusText: response.statusText,
        headers,
    });
};

const cachedAtOf = (response: Response): number | null => {
    const raw = response.headers.get(CACHED_AT_HEADER);

    return raw === null ? null : Number(raw);
};

/** URLs hashées : une correspondance en cache est vraie par construction, jamais revalidée. */
const cacheFirst = async (request: Request): Promise<Response> => {
    const cache = await caches.open(CACHE_NAME);
    const hit = await cache.match(request.url);

    if (hit !== undefined) {
        return hit;
    }

    const response = await fetch(request);

    if (response.ok) {
        await cache.put(request.url, stamped(response.clone()));
    }

    return response;
};

const networkFirst = async (request: Request): Promise<Response> => {
    const cache = await caches.open(CACHE_NAME);

    try {
        const response = await fetch(request);

        if (response.ok) {
            await cache.put(request.url, stamped(response.clone()));
            await broadcast({ type: 'FRESH' });
        }

        return response;
    } catch {
        const hit = await cache.match(request.url);

        if (hit !== undefined) {
            await broadcast({ type: 'SERVED_STALE', cachedAt: cachedAtOf(hit) });

            return hit;
        }

        const offline = await cache.match(self.OFFLINE_URL);

        return offline ?? Response.error();
    }
};

/**
 * Base de la réponse de secours : la réponse Inertia complète si elle est en cache, sinon la
 * charge utile portée par le document HTML.
 */
const cachedPagePayload = async (cache: Cache, shape: RequestShape): Promise<InertiaPage | null> => {
    const full = await cache.match(cacheKeyFor({ ...shape, partialData: null }));

    if (full !== undefined) {
        return (await full.json()) as InertiaPage;
    }

    const document = await cache.match(new URL(shape.url).toString());

    return document === undefined ? null : pagePayloadFromDocument(await document.text());
};

const rescuedResponse = async (cache: Cache, shape: RequestShape): Promise<Response> => {
    const page = await cachedPagePayload(cache, shape);

    if (page === null) {
        return Response.error();
    }

    await broadcast({ type: 'SERVED_STALE', cachedAt: null });

    return new Response(JSON.stringify(rescuedPartialPayload(page, partialKeysOf(shape.partialData))), {
        headers: {
            'Content-Type': 'application/json',
            'X-Inertia': 'true',
            Vary: 'X-Inertia',
        },
    });
};

const staleWhileRevalidate = async (request: Request, shape: RequestShape): Promise<Response> => {
    const cache = await caches.open(CACHE_NAME);
    const key = cacheKeyFor(shape);
    const hit = await cache.match(key);

    const network = fetch(request)
        .then(async (response: Response): Promise<Response | null> => {
            if (!response.ok) {
                return response;
            }

            await cache.put(key, stamped(response.clone()));
            await broadcast({ type: 'FRESH' });

            return response;
        })
        .catch((): null => null);

    if (hit !== undefined) {
        /** Revalidation en arrière-plan : son échec ne fait que signaler la péremption. */
        void network.then(async (response: Response | null): Promise<void> => {
            if (response === null) {
                await broadcast({ type: 'SERVED_STALE', cachedAt: cachedAtOf(hit) });
            }
        });

        return hit;
    }

    const response = await network;

    if (response !== null) {
        return response;
    }

    return shape.inertia && shape.partialData !== null
        ? rescuedResponse(cache, shape)
        : Response.error();
};

self.addEventListener('install', (event: ExtendableEvent): void => {
    event.waitUntil(
        caches.open(CACHE_NAME).then((cache: Cache): Promise<void> => cache.addAll(self.PRECACHE_URLS)),
    );
});

self.addEventListener('activate', (event: ExtendableEvent): void => {
    event.waitUntil((async (): Promise<void> => {
        const names = await caches.keys();

        await Promise.all(
            names
                .filter((name: string): boolean => name !== CACHE_NAME)
                .map((name: string): Promise<boolean> => caches.delete(name)),
        );

        await self.clients.claim();
    })());
});

self.addEventListener('message', (event: ExtendableMessageEvent): void => {
    if ((event.data as SwMessage | { type: string } | null)?.type === 'SKIP_WAITING') {
        void self.skipWaiting();
    }
});

self.addEventListener('fetch', (event: FetchEvent): void => {
    const shape = shapeOf(event.request);

    switch (classifyRequest(shape)) {
        case 'passthrough':
            return;
        case 'asset':
            event.respondWith(cacheFirst(event.request));

            return;
        case 'navigation':
            event.respondWith(networkFirst(event.request));

            return;
        default:
            event.respondWith(staleWhileRevalidate(event.request, shape));
    }
});
```

- [ ] **Step 7: Écrire le second config Vite**

Créer `vite.sw.config.ts` :

```ts
import { fileURLToPath, URL } from 'node:url';
import { defineConfig } from 'vite';

/**
 * Build séparé du service worker : il doit être servi depuis la racine pour avoir un scope `/`,
 * donc ni hash de nom ni découpage en chunks. Les greffons Laravel et Vue n'ont rien à y faire.
 */
export default defineConfig({
    resolve: {
        alias: {
            '@': fileURLToPath(new URL('./resources/js', import.meta.url)),
        },
    },
    build: {
        emptyOutDir: false,
        outDir: 'public',
        rollupOptions: {
            input: fileURLToPath(new URL('./resources/js/pwa/sw.ts', import.meta.url)),
            output: {
                format: 'iife',
                entryFileNames: 'sw-runtime.js',
                inlineDynamicImports: true,
            },
        },
    },
});
```

- [ ] **Step 8: Enchaîner les deux builds**

Dans `package.json`, remplacer la ligne du script `build` :

```json
        "build": "vite build && vite build --config vite.sw.config.ts",
```

- [ ] **Step 9: Compiler et vérifier la sortie**

Run: `bun run build`
Expected: `public/sw-runtime.js` existe, en un seul fichier.

Run: `test -f public/sw-runtime.js && head -c 80 public/sw-runtime.js`
Expected: le fichier existe et commence par du JavaScript compilé.

- [ ] **Step 10: Lancer les tests pour vérifier qu'ils passent**

Run: `php artisan test --compact --filter=PwaRoutes`
Expected: PASS — 5 tests.

Run: `bun run typecheck`
Expected: aucune erreur.

- [ ] **Step 11: Ignorer la sortie de build**

Vérifier que `public/sw-runtime.js` est ignoré par git au même titre que `public/build` :

```bash
grep -q '^/public/sw-runtime.js$' .gitignore || echo '/public/sw-runtime.js' >> .gitignore
```

- [ ] **Step 12: Formater et committer**

```bash
vendor/bin/pint --dirty --format agent
git add app/Shared/Pwa/ServiceWorkerScript.php resources/js/pwa/sw.ts vite.sw.config.ts resources/views/pwa/sw.blade.php routes/pwa.php package.json tests/Feature/PwaRoutesTest.php tests/Pest.php .gitignore
git commit -m "feat: sert un service worker compilé et versionné par le manifest Vite"
```

---

## Task 4 : État PWA côté client

**Files:**
- Create: `resources/js/lib/serviceWorker.ts`
- Create: `resources/js/lib/serviceWorker.test.ts`

**Interfaces:**
- Consomme : les messages `{ type: 'FRESH' }` et `{ type: 'SERVED_STALE', cachedAt }` diffusés par le worker (Task 3) ; le message `{ type: 'SKIP_WAITING' }` qu'il accepte.
- Produit, consommés en Task 5 :
  - `updateAvailable: Ref<boolean>`, `stale: Ref<boolean>`, `lastSyncedAt: Ref<number | null>`, `canInstall: Ref<boolean>`
  - `registerServiceWorker(): void`
  - `trackRegistration(registration: ServiceWorkerRegistration): void`
  - `handleMessage(message: SwClientMessage): void`
  - `handleControllerChange(): boolean`
  - `applyUpdate(): void`
  - `handleBeforeInstallPrompt(event: BeforeInstallPromptEvent): void`
  - `promptInstall(): Promise<void>`
  - `dismissInstall(): void`
  - `setReloader(fn: () => void): void`

- [ ] **Step 1: Écrire les tests qui échouent**

Créer `resources/js/lib/serviceWorker.test.ts` :

```ts
import { beforeEach, describe, expect, it, vi } from 'vitest';
import {
    applyUpdate,
    canInstall,
    dismissInstall,
    handleBeforeInstallPrompt,
    handleControllerChange,
    handleMessage,
    lastSyncedAt,
    promptInstall,
    setReloader,
    stale,
    trackRegistration,
    updateAvailable,
    type BeforeInstallPromptEvent,
} from '@/lib/serviceWorker';

/** Enregistrement minimal : seul `waiting` et l'écoute d'`updatefound` sont sollicités. */
const registrationWithWaiting = (waiting: { postMessage: (data: unknown) => void }) => ({
    waiting,
    installing: null,
    addEventListener: (): void => {},
}) as unknown as ServiceWorkerRegistration;

const installPromptEvent = (outcome: 'accepted' | 'dismissed'): BeforeInstallPromptEvent => ({
    preventDefault: vi.fn(),
    prompt: vi.fn().mockResolvedValue(undefined),
    userChoice: Promise.resolve({ outcome }),
}) as unknown as BeforeInstallPromptEvent;

beforeEach(() => {
    updateAvailable.value = false;
    stale.value = false;
    lastSyncedAt.value = null;
    canInstall.value = false;
    localStorage.clear();
    setReloader(() => {});
});

describe('trackRegistration', () => {
    it('signale une mise à jour quand un worker attend déjà', () => {
        trackRegistration(registrationWithWaiting({ postMessage: vi.fn() }));

        expect(updateAvailable.value).toBe(true);
    });
});

describe('applyUpdate', () => {
    it('demande au worker en attente de prendre la main', () => {
        const postMessage = vi.fn();
        trackRegistration(registrationWithWaiting({ postMessage }));

        applyUpdate();

        expect(postMessage).toHaveBeenCalledWith({ type: 'SKIP_WAITING' });
    });

    it('ne recharge que le changement de contrôleur qu\'il a lui-même déclenché', () => {
        const reload = vi.fn();
        setReloader(reload);

        expect(handleControllerChange()).toBe(false);

        trackRegistration(registrationWithWaiting({ postMessage: vi.fn() }));
        applyUpdate();

        expect(handleControllerChange()).toBe(true);
        expect(reload).toHaveBeenCalledTimes(1);
    });

    it('ne recharge pas deux fois de suite', () => {
        const reload = vi.fn();
        setReloader(reload);
        trackRegistration(registrationWithWaiting({ postMessage: vi.fn() }));
        applyUpdate();

        handleControllerChange();
        handleControllerChange();

        expect(reload).toHaveBeenCalledTimes(1);
    });
});

describe('handleMessage', () => {
    it('marque les données comme périmées et retient leur horodatage', () => {
        handleMessage({ type: 'SERVED_STALE', cachedAt: 1_760_000_000_000 });

        expect(stale.value).toBe(true);
        expect(lastSyncedAt.value).toBe(1_760_000_000_000);
    });

    it('efface la péremption dès qu\'une revalidation réussit', () => {
        handleMessage({ type: 'SERVED_STALE', cachedAt: 1_760_000_000_000 });

        handleMessage({ type: 'FRESH' });

        expect(stale.value).toBe(false);
    });
});

describe('invite d\'installation', () => {
    it('neutralise l\'événement natif et propose l\'installation', () => {
        const event = installPromptEvent('accepted');

        handleBeforeInstallPrompt(event);

        expect(event.preventDefault).toHaveBeenCalled();
        expect(canInstall.value).toBe(true);
    });

    it('ne repropose rien après un refus mémorisé', () => {
        dismissInstall();

        handleBeforeInstallPrompt(installPromptEvent('accepted'));

        expect(canInstall.value).toBe(false);
    });

    it('mémorise un refus exprimé dans l\'invite native', async () => {
        handleBeforeInstallPrompt(installPromptEvent('dismissed'));

        await promptInstall();

        expect(canInstall.value).toBe(false);

        canInstall.value = false;
        handleBeforeInstallPrompt(installPromptEvent('accepted'));

        expect(canInstall.value).toBe(false);
    });
});
```

- [ ] **Step 2: Lancer les tests pour vérifier qu'ils échouent**

Run: `bunx vitest run resources/js/lib/serviceWorker.test.ts`
Expected: FAIL — « Failed to resolve import "@/lib/serviceWorker" ».

- [ ] **Step 3: Écrire l'implémentation**

Créer `resources/js/lib/serviceWorker.ts` :

```ts
import { ref, type Ref } from 'vue';

/** Type non standardisé, absent de la bibliothèque DOM, mais implémenté par Chrome. */
export type BeforeInstallPromptEvent = Event & {
    prompt: () => Promise<void>;
    userChoice: Promise<{ outcome: 'accepted' | 'dismissed' }>;
};

export type SwClientMessage =
    | { type: 'FRESH' }
    | { type: 'SERVED_STALE'; cachedAt: number | null };

/** Un refus est définitif : l'invite d'installation ne se représente pas à chaque visite. */
const DISMISSED_KEY = 'pwa-install-dismissed';

export const updateAvailable: Ref<boolean> = ref(false);
export const stale: Ref<boolean> = ref(false);
export const lastSyncedAt: Ref<number | null> = ref(null);
export const canInstall: Ref<boolean> = ref(false);

let waitingWorker: ServiceWorker | null = null;
let installEvent: BeforeInstallPromptEvent | null = null;

/** Vrai seulement entre notre `SKIP_WAITING` et le `controllerchange` qui en découle. */
let awaitingReload = false;

let reload: () => void = (): void => {
    window.location.reload();
};

/** Point d'injection : happy-dom n'a pas de vraie navigation à recharger. */
export function setReloader(fn: () => void): void {
    reload = fn;
}

export function trackRegistration(registration: ServiceWorkerRegistration): void {
    if (registration.waiting !== null) {
        waitingWorker = registration.waiting;
        updateAvailable.value = true;
    }

    registration.addEventListener('updatefound', (): void => {
        const installing = registration.installing;

        if (installing === null) {
            return;
        }

        installing.addEventListener('statechange', (): void => {
            /** Sans contrôleur, c'est la toute première installation : rien à proposer. */
            if (installing.state !== 'installed' || navigator.serviceWorker.controller === null) {
                return;
            }

            waitingWorker = installing;
            updateAvailable.value = true;
        });
    });
}

export function handleMessage(message: SwClientMessage): void {
    if (message.type === 'FRESH') {
        stale.value = false;
        lastSyncedAt.value = Date.now();

        return;
    }

    stale.value = true;
    lastSyncedAt.value = message.cachedAt;
}

/** Renvoie vrai si un rechargement a été demandé — valeur de retour destinée aux tests. */
export function handleControllerChange(): boolean {
    if (!awaitingReload) {
        return false;
    }

    awaitingReload = false;
    reload();

    return true;
}

export function applyUpdate(): void {
    if (waitingWorker === null) {
        return;
    }

    awaitingReload = true;
    waitingWorker.postMessage({ type: 'SKIP_WAITING' });
}

export function handleBeforeInstallPrompt(event: BeforeInstallPromptEvent): void {
    event.preventDefault();
    installEvent = event;
    canInstall.value = localStorage.getItem(DISMISSED_KEY) === null;
}

export async function promptInstall(): Promise<void> {
    if (installEvent === null) {
        return;
    }

    await installEvent.prompt();
    const { outcome } = await installEvent.userChoice;

    if (outcome === 'dismissed') {
        localStorage.setItem(DISMISSED_KEY, '1');
    }

    installEvent = null;
    canInstall.value = false;
}

export function dismissInstall(): void {
    localStorage.setItem(DISMISSED_KEY, '1');
    canInstall.value = false;
}

/**
 * Inertia ne fait aucune vraie navigation, donc le navigateur ne re-télécharge jamais `/sw.js`
 * de lui-même : sans ces deux déclencheurs, une application installée peut rester des jours
 * sur l'ancienne version.
 */
function scheduleUpdateChecks(registration: ServiceWorkerRegistration): void {
    document.addEventListener('visibilitychange', (): void => {
        if (document.visibilityState === 'visible') {
            void registration.update();
        }
    });

    document.addEventListener('inertia:navigate', (): void => {
        void registration.update();
    });
}

export function registerServiceWorker(): void {
    if (!('serviceWorker' in navigator)) {
        return;
    }

    window.addEventListener('beforeinstallprompt', (event: Event): void => {
        handleBeforeInstallPrompt(event as BeforeInstallPromptEvent);
    });

    navigator.serviceWorker.addEventListener('message', (event: MessageEvent): void => {
        handleMessage(event.data as SwClientMessage);
    });

    navigator.serviceWorker.addEventListener('controllerchange', (): void => {
        handleControllerChange();
    });

    void navigator.serviceWorker
        .register('/sw.js')
        .then((registration: ServiceWorkerRegistration): void => {
            trackRegistration(registration);
            scheduleUpdateChecks(registration);
        });
}
```

- [ ] **Step 4: Lancer les tests pour vérifier qu'ils passent**

Run: `bunx vitest run resources/js/lib/serviceWorker.test.ts`
Expected: PASS — 9 tests.

- [ ] **Step 5: Vérifier les types**

Run: `bun run typecheck`
Expected: aucune erreur.

- [ ] **Step 6: Committer**

```bash
git add resources/js/lib/serviceWorker.ts resources/js/lib/serviceWorker.test.ts
git commit -m "feat: expose l'état PWA au client"
```

---

## Task 5 : Bandeau et montage

**Files:**
- Create: `resources/js/components/AppServiceWorkerBanner.vue`
- Create: `resources/js/pwa/banner.ts`
- Create: `tests/Browser/PwaTest.php`
- Modify: `resources/js/app.ts`

**Interfaces:**
- Consomme : tout l'état exporté par `@/lib/serviceWorker` (Task 4) ; le nœud `#pwa-banner` (Task 1) ; le worker servi à `/sw.js` (Task 3).
- Produit : `mountServiceWorkerBanner(): void`, appelé une fois depuis `app.ts`. Attributs de test stables : `[data-pwa-banner="update|stale|install"]`, `[data-pwa-action="reload|refresh|install|dismiss"]`.

- [ ] **Step 1: Écrire le test qui échoue**

Créer `tests/Browser/PwaTest.php`. Le `beforeEach` est indispensable : tant que `public/hot` existe, Vite est en mode développement, `manifestHash()` est nul et la route ne sert que le worker inerte.

```php
<?php

/** Aides définies dans `tests/Pest.php` en Task 3. */
beforeEach(function () {
    $this->viteHotFile = hideViteHotFile();
});

afterEach(function () {
    restoreViteHotFile($this->viteHotFile);
});

it('enregistre le service worker et remplit son cache', function () {
    ['user' => $user] = portfolioFixture();

    $this->actingAs($user);

    visit('/')
        ->assertScript("typeof navigator.serviceWorker !== 'undefined'", true)
        ->assertScript(
            'navigator.serviceWorker.ready.then((registration) => registration.active !== null)',
            true,
        )
        ->assertScript(
            "caches.keys().then((names) => names.some((name) => name.startsWith('argent-')))",
            true,
        )
        ->assertNoJavaScriptErrors();
});

it('propose l\'installation quand le navigateur la signale', function () {
    ['user' => $user] = portfolioFixture();

    $this->actingAs($user);

    $page = visit('/');

    $page->script(<<<'JS'
        const event = new Event('beforeinstallprompt');
        event.prompt = () => Promise.resolve();
        event.userChoice = Promise.resolve({ outcome: 'accepted' });
        window.dispatchEvent(event);
    JS);

    $page
        ->assertScript("document.querySelector('[data-pwa-banner=install]') !== null", true)
        ->assertSee('Installer l\'app')
        ->assertNoJavaScriptErrors();
});
```

- [ ] **Step 2: Lancer le test pour vérifier qu'il échoue**

Run: `bun run build && php artisan test --compact --filter=PwaTest`
Expected: FAIL — aucun worker enregistré, aucun cache `argent-*`, aucun nœud `[data-pwa-banner=install]`.

- [ ] **Step 3: Écrire le bandeau**

Créer `resources/js/components/AppServiceWorkerBanner.vue` :

```vue
<script setup lang="ts">
import { computed } from 'vue';
import {
    applyUpdate,
    canInstall,
    dismissInstall,
    lastSyncedAt,
    promptInstall,
    stale,
    updateAvailable,
} from '@/lib/serviceWorker';

type BannerState = 'update' | 'stale' | 'install' | 'none';

/**
 * Priorité stricte : recharger pour une nouvelle version règle aussi la fraîcheur, donc les
 * deux premiers états n'ont jamais à cohabiter.
 */
const state = computed<BannerState>(() => {
    if (updateAvailable.value) {
        return 'update';
    }

    if (stale.value) {
        return 'stale';
    }

    return canInstall.value ? 'install' : 'none';
});

const syncedLabel = computed<string>(() => {
    if (lastSyncedAt.value === null) {
        return 'Données hors-ligne';
    }

    const date = new Date(lastSyncedAt.value);
    const day = date.toLocaleDateString('fr-FR', { day: '2-digit', month: '2-digit' });
    const time = date.toLocaleTimeString('fr-FR', { hour: '2-digit', minute: '2-digit' });

    return `Données du ${day} à ${time}`;
});

const refresh = (): void => {
    window.location.reload();
};
</script>

<template>
    <!-- Position fixe : le bandeau ne décale pas la mise en page, donc ne casse ni le carrousel ni la hauteur des graphes. -->
    <div
        v-if="state !== 'none'"
        :data-pwa-banner="state"
        class="fixed inset-x-0 bottom-0 z-50 mx-auto flex w-full max-w-[520px] items-center justify-between gap-3 rounded-t-xl bg-foreground px-4 py-3 text-sm text-background shadow-lg"
    >
        <template v-if="state === 'update'">
            <span>Nouvelle version disponible</span>
            <button
                type="button"
                data-pwa-action="reload"
                class="shrink-0 rounded-md bg-background px-3 py-1.5 font-medium text-foreground"
                @click="applyUpdate"
            >
                Recharger
            </button>
        </template>

        <template v-else-if="state === 'stale'">
            <span>{{ syncedLabel }}</span>
            <button
                type="button"
                data-pwa-action="refresh"
                class="shrink-0 rounded-md bg-background px-3 py-1.5 font-medium text-foreground"
                @click="refresh"
            >
                Actualiser
            </button>
        </template>

        <template v-else>
            <span>Installer l'app</span>
            <div class="flex shrink-0 items-center gap-2">
                <button
                    type="button"
                    data-pwa-action="dismiss"
                    class="px-2 py-1.5 opacity-70"
                    @click="dismissInstall"
                >
                    Plus tard
                </button>
                <button
                    type="button"
                    data-pwa-action="install"
                    class="rounded-md bg-background px-3 py-1.5 font-medium text-foreground"
                    @click="promptInstall"
                >
                    Installer
                </button>
            </div>
        </template>
    </div>
</template>
```

- [ ] **Step 4: Écrire le montage**

Créer `resources/js/pwa/banner.ts` :

```ts
import { createApp } from 'vue';
import AppServiceWorkerBanner from '@/components/AppServiceWorkerBanner.vue';
import { registerServiceWorker } from '@/lib/serviceWorker';

/**
 * Application Vue distincte : les pages Inertia sont des racines indépendantes, sans layout
 * partagé, donc le bandeau serait sinon à dupliquer dans chacune.
 */
export function mountServiceWorkerBanner(): void {
    const host = document.getElementById('pwa-banner');

    if (host === null) {
        return;
    }

    registerServiceWorker();
    createApp(AppServiceWorkerBanner).mount(host);
}
```

- [ ] **Step 5: Appeler le montage depuis `app.ts`**

Dans `resources/js/app.ts`, ajouter l'import auprès des autres et l'appel après `useSystemTheme()` :

```ts
import { useSystemTheme } from '@/lib/theme';
import { mountServiceWorkerBanner } from '@/pwa/banner';

const appName = import.meta.env.VITE_APP_NAME ?? 'Laravel';

useSystemTheme();
mountServiceWorkerBanner();
```

- [ ] **Step 6: Lancer les tests pour vérifier qu'ils passent**

Run: `bun run build && php artisan test --compact --filter=PwaTest`
Expected: PASS — 2 tests.

- [ ] **Step 7: Vérifier que rien n'a régressé**

Run: `php artisan test --compact --filter=Smoke`
Expected: PASS — 3 tests. Le bandeau ne doit provoquer aucune erreur JavaScript sur les pages existantes.

Run: `bun run typecheck`
Expected: aucune erreur.

- [ ] **Step 8: Committer**

```bash
git add resources/js/components/AppServiceWorkerBanner.vue resources/js/pwa/banner.ts resources/js/app.ts tests/Browser/PwaTest.php
git commit -m "feat: affiche l'état PWA dans un bandeau flottant"
```

---

## Task 6 : Sections dégradées hors-ligne

**Files:**
- Modify: `resources/js/components/ValueVsInvestedChart.vue`
- Modify: `resources/js/components/dashboard/PerformancesSection.vue`
- Modify: `resources/js/components/dashboard/SectorsSection.vue`
- Modify: `resources/js/components/instrument/PriceHistorySection.vue`
- Modify: `resources/js/components/dashboard/InstrumentsSection.vue`
- Modify: `resources/js/lib/instrumentList.ts`
- Test: `resources/js/lib/instrumentList.test.ts`

**Interfaces:**
- Consomme : `page.rescuedProps`, champ de premier niveau du payload Inertia, exposé par `usePage()` ; les réponses de secours produites par le worker (Task 3).
- Produit : `isCatalogLoading(trends: CatalogTrend[] | undefined, rescuedProps: string[] | undefined, reloading: boolean): boolean` dans `@/lib/instrumentList`. Le rendu des slots `#rescue` est vérifié de bout en bout en Task 7.

**Contexte.** `Deferred` d'Inertia v3 rend son slot `#rescue` dès qu'une de ses clés figure dans `page.rescuedProps`, et cela même quand la prop est absente. La priorité de rendu est : `default` si toutes les props sont définies et aucune n'est rescapée, sinon `rescue` s'il existe, sinon `fallback`.

Les quatre `<Deferred>` n'ont donc besoin que d'un slot supplémentaire, sans logique — leur rendu se vérifie dans le vrai parcours hors-ligne de la Task 7, où le worker produit une vraie réponse rescapée. Le seul comportement décidable est celui d'`InstrumentsSection`, qui n'utilise pas `<Deferred>` : il est extrait en fonction pure et testé ici.

- [ ] **Step 1: Écrire le test qui échoue**

Ajouter à `resources/js/lib/instrumentList.test.ts` :

```ts
import { isCatalogLoading } from '@/lib/instrumentList';

describe('isCatalogLoading', () => {
    it('charge tant que les tendances ne sont pas arrivées', () => {
        expect(isCatalogLoading(undefined, undefined, false)).toBe(true);
    });

    it('arrête de charger dès que les tendances sont là', () => {
        expect(isCatalogLoading([], undefined, false)).toBe(false);
    });

    it('arrête de charger quand le worker a rescapé les tendances', () => {
        expect(isCatalogLoading(undefined, ['trends'], false)).toBe(false);
    });

    it('ignore une clé rescapée qui ne la concerne pas', () => {
        expect(isCatalogLoading(undefined, ['sectorBreakdown'], false)).toBe(true);
    });

    it('recharge en priorité sur tout le reste', () => {
        expect(isCatalogLoading([], ['trends'], true)).toBe(true);
    });
});
```

Vérifier que `describe`, `expect` et `it` figurent déjà dans l'import `vitest` en tête du fichier ; les ajouter sinon.

- [ ] **Step 2: Lancer le test pour vérifier qu'il échoue**

Run: `bunx vitest run resources/js/lib/instrumentList.test.ts`
Expected: FAIL — « isCatalogLoading is not a function ».

- [ ] **Step 3: Écrire la fonction pure**

Ajouter à la fin de `resources/js/lib/instrumentList.ts` :

```ts
/**
 * Le catalogue arrive différé, donc son squelette tient sur `trends === undefined`. Hors-ligne
 * la prop n'arrivera jamais : le worker rescape sa clé, et le squelette n'a plus à tourner.
 */
export function isCatalogLoading(
    trends: CatalogTrend[] | undefined,
    rescuedProps: string[] | undefined,
    reloading: boolean,
): boolean {
    if (reloading) {
        return true;
    }

    return trends === undefined && !(rescuedProps ?? []).includes('trends');
}
```

Si `CatalogTrend` n'est pas déjà importé dans ce fichier, l'ajouter : `import type { CatalogTrend } from '@/lib/catalog';`.

- [ ] **Step 4: Lancer les tests pour vérifier qu'ils passent**

Run: `bunx vitest run resources/js/lib/instrumentList.test.ts`
Expected: PASS — les 5 nouveaux tests plus les tests existants du fichier.

- [ ] **Step 5: Ajouter le slot au graphe partagé**

Dans `resources/js/components/ValueVsInvestedChart.vue`, ajouter le slot juste après `#fallback` — ce fichier sert à la fois `evolutionSeries` sur le tableau de bord et `valuation` sur la fiche instrument :

```vue
            <template #rescue>
                <p class="py-8 text-center text-sm text-muted-foreground">
                    Données indisponibles hors-ligne.
                </p>
            </template>
```

- [ ] **Step 6: Ajouter le slot aux performances**

Dans `resources/js/components/dashboard/PerformancesSection.vue`, après `#fallback` :

```vue
            <template #rescue>
                <p class="py-8 text-center text-sm text-muted-foreground">
                    Données indisponibles hors-ligne.
                </p>
            </template>
```

- [ ] **Step 7: Ajouter le slot aux secteurs**

Dans `resources/js/components/dashboard/SectorsSection.vue`, après `#fallback` :

```vue
            <template #rescue>
                <p class="py-8 text-center text-sm text-muted-foreground">
                    Données indisponibles hors-ligne.
                </p>
            </template>
```

- [ ] **Step 8: Ajouter le slot à l'historique de prix**

Dans `resources/js/components/instrument/PriceHistorySection.vue`, après `#fallback` :

```vue
                <template #rescue>
                    <p class="py-8 text-center text-sm text-muted-foreground">
                        Données indisponibles hors-ligne.
                    </p>
                </template>
```

- [ ] **Step 9: Brancher la liste d'instruments sur la fonction pure**

Dans `resources/js/components/dashboard/InstrumentsSection.vue`, ajouter `usePage` à l'import Inertia existant :

```ts
import { router, usePage } from '@inertiajs/vue3';
```

ajouter `isCatalogLoading` à l'import déjà présent depuis `@/lib/instrumentList` :

```ts
import { instrumentSections, isCatalogLoading, mergeInstrumentRows, type InstrumentSection } from '@/lib/instrumentList';
```

puis remplacer le calcul de `loading` :

```ts
const page = usePage();

/** Le catalogue et ses tendances arrivent différés ; seul le squelette des tendances est visible. */
const loading = computed<boolean>(() => isCatalogLoading(
    props.trends,
    page.props.rescuedProps as string[] | undefined,
    reloading.value,
));
```

- [ ] **Step 10: Vérifier que rien n'a régressé**

Run: `bunx vitest run`
Expected: PASS — toute la suite JavaScript.

Run: `bun run build && php artisan test --compact --filter=Smoke`
Expected: PASS — 3 tests. Les slots `#rescue` ne doivent rien changer tant que rien n'est rescapé.

Run: `bun run typecheck`
Expected: aucune erreur.

- [ ] **Step 11: Committer**

```bash
git add resources/js/lib/instrumentList.ts resources/js/lib/instrumentList.test.ts resources/js/components/ValueVsInvestedChart.vue resources/js/components/dashboard/PerformancesSection.vue resources/js/components/dashboard/SectorsSection.vue resources/js/components/instrument/PriceHistorySection.vue resources/js/components/dashboard/InstrumentsSection.vue
git commit -m "feat: dégrade les sections différées indisponibles hors-ligne"
```

---

## Task 7 : Parcours hors-ligne de bout en bout

**Files:**
- Create: `tests/pwa-offline.mjs`
- Modify: `package.json`

**Interfaces:**
- Consomme : l'application complète des tâches 1 à 6, servie par Herd à `https://argent.test`.
- Produit : le script npm `test:pwa`.

**Contexte.** `Pest\Browser\Playwright\Context` n'expose pas `setOffline`, et l'atteindre passerait par des classes marquées `@internal`. Ce dernier scénario s'appuie donc directement sur `playwright`, déjà présent dans `devDependencies`.

- [ ] **Step 1: Écrire le script**

Créer `tests/pwa-offline.mjs` :

```js
import assert from 'node:assert/strict';
import { chromium } from 'playwright';

const BASE_URL = process.env.PWA_BASE_URL ?? 'https://argent.test';

const browser = await chromium.launch();
const context = await browser.newContext({ ignoreHTTPSErrors: true });
const page = await context.newPage();

try {
    await page.goto(BASE_URL, { waitUntil: 'networkidle' });

    /** Le worker doit être actif et son précache rempli avant qu'on coupe le réseau. */
    await page.waitForFunction(() => navigator.serviceWorker.controller !== null);
    await page.waitForFunction(
        () => caches.keys().then((names) => names.some((name) => name.startsWith('argent-'))),
    );

    await context.setOffline(true);
    await page.reload({ waitUntil: 'domcontentloaded' });

    const body = await page.textContent('body');
    assert.ok(body.includes('Performances'), 'La page en cache doit rester lisible hors-ligne.');
    assert.ok(!body.includes('Pas de connexion'), 'La page hors-ligne ne doit pas remplacer une page en cache.');

    await page.waitForSelector('[data-pwa-banner="stale"]', { timeout: 10_000 });

    /**
     * Les partiels différés ont été mis en cache pendant la visite en ligne. On les retire pour
     * atteindre le cas que le slot `#rescue` doit couvrir : la page est là, ses groupes ne le
     * sont pas.
     */
    await context.setOffline(false);
    await page.evaluate(async () => {
        const [name] = (await caches.keys()).filter((key) => key.startsWith('argent-'));
        const cache = await caches.open(name);
        const keys = await cache.keys();

        await Promise.all(
            keys
                .filter((request) => request.url.includes('__sw=partial'))
                .map((request) => cache.delete(request)),
        );
    });
    await context.setOffline(true);
    await page.reload({ waitUntil: 'domcontentloaded' });

    await page.waitForSelector('text=Données indisponibles hors-ligne', { timeout: 10_000 });

    /** Une URL jamais visitée n'a rien en cache : c'est le repli qui doit apparaître. */
    await page.goto(`${BASE_URL}/instruments/999999`, { waitUntil: 'domcontentloaded' });
    assert.ok(
        (await page.textContent('body')).includes('Pas de connexion'),
        'Une page jamais visitée doit tomber sur le repli hors-ligne.',
    );

    console.log('Parcours hors-ligne : OK');
} finally {
    await context.close();
    await browser.close();
}
```

- [ ] **Step 2: Déclarer le script npm**

Dans `package.json`, ajouter après la ligne `"test:js:watch"` :

```json
        "test:pwa": "node tests/pwa-offline.mjs"
```

- [ ] **Step 3: Construire puis lancer le script**

Arrêter d'abord `bun run dev` s'il tourne, puis supprimer `public/hot` : tant que ce fichier existe, `Vite::manifestHash()` est nul et la route ne sert que le worker inerte.

Run: `rm -f public/hot && bun run build && bun run test:pwa`
Expected: `Parcours hors-ligne : OK`, sortie sans erreur.

Si le script échoue sur l'attente du worker, vérifier les deux causes possibles : `public/sw-runtime.js` absent, ou `public/hot` présent. Dans les deux cas la route sert le worker inerte, qui se désenregistre.

- [ ] **Step 4: Vérifier la suite complète**

Run: `php artisan test --compact`
Expected: PASS.

Run: `bunx vitest run`
Expected: PASS.

Run: `bun run typecheck`
Expected: aucune erreur.

- [ ] **Step 5: Committer**

```bash
git add tests/pwa-offline.mjs package.json
git commit -m "test: vérifie le parcours hors-ligne dans un vrai navigateur"
```

---

## Vérification finale

- [ ] `bun run build` produit `public/build/manifest.json` et `public/sw-runtime.js`.
- [ ] `php artisan test --compact` passe intégralement.
- [ ] `bunx vitest run` passe intégralement.
- [ ] `bun run typecheck` ne signale rien.
- [ ] `vendor/bin/pint --dirty --format agent` ne modifie plus rien.
- [ ] Dans Chrome, onglet Application : le manifest est reconnu, le worker est `activated`, le cache `argent-<hash>` contient les assets, et « Installer » est proposé.
- [ ] Après un nouveau `bun run build`, revenir au premier plan fait apparaître le bandeau « Nouvelle version disponible » ; « Recharger » applique la mise à jour une seule fois.
