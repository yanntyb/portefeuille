/** Une seule forme d'option pour les deux sélecteurs : un appelant n'a qu'un type à importer. */
export type { SelectOption } from "@/components/ui/native-select"
export { default as SearchSelect } from "./SearchSelect.vue"
